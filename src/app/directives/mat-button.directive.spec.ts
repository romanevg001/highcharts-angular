import { MatButtonDirective } from './mat-button.directive';

describe('MatButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MatButtonDirective();
    expect(directive).toBeTruthy();
  });
});
